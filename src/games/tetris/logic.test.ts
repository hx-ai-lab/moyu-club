import{describe,expect,it}from'vitest';import{clearLines,collides,createPiece,emptyBoard,gameOver,movePiece,rotatePiece}from'./logic';
describe('tetris',()=>{it('moves a piece',()=>{const b=emptyBoard(),p=createPiece(()=>0);expect(movePiece(b,p,1,0).x).toBe(p.x+1)});it('blocks board boundaries',()=>{const b=emptyBoard(),p={...createPiece(()=>0),x:0};expect(movePiece(b,p,-1,0).x).toBe(0);expect(collides(b,{...p,y:20})).toBe(true)});it('rotates pieces',()=>{const b=emptyBoard(),p=createPiece(()=>0);expect(rotatePiece(b,p).shape.length).toBe(4)});it('clears full lines',()=>{const b=emptyBoard();b[19].fill(1);const r=clearLines(b);expect(r.count).toBe(1);expect(r.board[0].every(v=>v===0)).toBe(true)});it('detects game over',()=>{const b=emptyBoard(),p=createPiece(()=>0);b[0][p.x]=1;expect(gameOver(b,p)).toBe(true)})})
