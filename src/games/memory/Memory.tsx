import { useEffect,useState } from 'react';import { createDeck,isMatch,memoryWon } from './logic';import type { Card } from './types'
export function Memory({onBack}:{onBack:()=>void}){const [cards,setCards]=useState<Card[]>(createDeck);const [open,setOpen]=useState<number[]>([]);const [moves,setMoves]=useState(0);const [time,setTime]=useState(0);const [started,setStarted]=useState(false);const won=memoryWon(cards);useEffect(()=>{if(!started||won)return;const id=setInterval(()=>setTime(t=>t+1),1000);return()=>clearInterval(id)},[started,won]);useEffect(()=>{if(open.length!==2)return;const id=setTimeout(()=>{const[a,b]=open;if(isMatch(cards[a],cards[b]))setCards(cs=>cs.map((c,i)=>i===a||i===b?{...c,matched:true}:c));setOpen([])},700);return()=>clearTimeout(id)},[open,cards]);const flip=(i:number)=>{if(open.length===2||open.includes(i)||cards[i].matched)return;setStarted(true);setOpen(v=>[...v,i]);if(open.length===1)setMoves(m=>m+1)};const reset=()=>{setCards(createDeck());setOpen([]);setMoves(0);setTime(0);setStarted(false)};return <main className="game-page memory-page"><nav><button className="icon-button" onClick={onBack}>←</button><b>记忆翻牌</b><button className="text-button" onClick={reset}>重新开始</button></nav><div className="game-stats"><b>步数 {moves}</b><b>用时 {time}s</b></div><div className="memory-board">{cards.map((c,i)=>{const shown=c.matched||open.includes(i);return <button className={shown?'shown':''} onClick={()=>flip(i)} aria-label={shown?c.symbol:'未翻开的卡片'}><span>{shown?c.symbol:'M'}</span></button>})}</div><p className="game-message">{won?'全部配对成功！':'一次翻开两张相同图案'}</p></main>}
